{
  "postCreateCommand": "npm ci",
  "context": "..",
  "remoteUser": "vscode",
  "forwardPorts": [
    3000
  ],
  "postStartCommand": "npm test",
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.shell.linux": "/bin/bash",
        "terminal.integrated.env.linux": {
          "CI": "true"
        }
      }
    }
  },
  "name": "tic-proyecto Dev Container",
  "dockerFile": "Dockerfile"
}
